#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    vector<int> decrypt(vector<int>& code, int k) {
        vector<int> now;
        if(k == 0){
            for(int i = 0 ; i < code.size() ; i++){
                now.push_back(0);
            }
        }if(k > 0){
            for(int i = 1 ; i < k ; i++){
                
            }
        }
    }
};